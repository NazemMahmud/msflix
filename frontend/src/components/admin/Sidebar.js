import React from "react";

function Sidebar(props) {
    console.log("props: ", props);
    return (
        <React.Fragment>
           <div>

           </div>
        </React.Fragment>
    );
};

export default Sidebar;